@model ExMark.ViewModel.ScheduleViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class='row'>
    <div class='span3'>
        @{
            Html.RenderAction("StudentProfile", "Student", Model.Student.Id);
        }
        
        <br>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Recent Messages <a href='../messages/messages.html'>see all</a></th>
                </tr>
            </thead>
            <tbody>
                <tr class='em-date-row'>
                    <td>Трололо</td>
                </tr>
            </tbody>
        </table>
    </div>
            <div class='span9'>
                @{ Html.RenderAction("List", "Parent", new {progresses = Model.Progresses}); }
            </div>
        </div>